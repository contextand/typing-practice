# CLAUDE.md

## 1. 이 프로젝트가 무엇인가

한국어 타이핑 연습 사이트. 책에서 발췌한 문장을 랜덤으로 보여주고 따라 치게 한다.

- `/` — 타이핑 연습 화면 (PC는 좌우 분할, 모바일은 카드형)
- `/book` — 관리자용 책 목록 페이지. 전체 책과 발췌문을 눈으로 확인하는 용도

## 2. 가장 먼저 알아야 할 것

**`index.html`과 `book/index.html`은 손으로 고치지 말 것.**

둘 다 `build.py`가 생성하는 산출물이다. 직접 수정하면 다음 자동 배포 때 통째로 덮어써진다.
화면에 보이는 무언가를 바꾸려면 항상 `build.py` 안의 템플릿을 고치고 다시 빌드한다.

```bash
python3 build.py   # index.html, book/index.html 두 개를 생성
```

## 3. 데이터가 흐르는 길

```
노션 (사용자의 1차 기록)
  → Sanity CMS (콘텐츠 원본 = source of truth)
    → build.py 가 빌드 시점에 fetch
      → index.html / book/index.html 생성
        → git push
          → Vercel 자동 배포
```

로컬 파일에서 콘텐츠를 읽지 않는다. 빌드할 때마다 Sanity API를 호출하므로 네트워크가 필요하다.

### Sanity

- 프로젝트 ID `piwxcoph`, 데이터셋 `production` (공개 읽기 — 토큰 없이 조회 가능)
- Studio(입력 화면): https://typing-books.sanity.studio/
- 문서 타입 두 가지

  | 타입 | 필드 | 용도 |
  |---|---|---|
  | `book` | title, author, genre, publisher, organizedDate, passages[] | 책 발췌문 |
  | `quote` | name, quotes[] | '발췌의 발췌'. 이름(예: `애플저널 210904`)으로 묶인 짧은 문장 모음 |

  `quote`는 이름 하나에 문장 여러 개가 배열로 들어간다. 낱개 문서로 만들지 않는다.

### 자동 배포

Sanity에서 Publish → 웹훅이 GitHub `repository_dispatch`(`sanity-publish`)를 호출 →
`.github/workflows/deploy.yml`이 빌드하고 커밋·푸시 → Vercel이 배포. 대략 1분 걸린다.

- 웹훅 이름 `deploy-to-github`, 필터 `_type in ["book", "quote"]`
- 웹훅의 Authorization 헤더에 GitHub PAT가 들어있다 (저장소에는 없음)
- 워크플로우에 `concurrency` 그룹이 있어 실행이 겹치지 않고 순서대로 처리된다

**⚠️ build.py가 생성하는 파일을 늘리면, 워크플로우의 `git add` 목록에도 반드시 추가할 것.**
빠뜨리면 스테이징되지 않은 파일 때문에 `git pull --rebase`가 중단되어 이후 모든 자동 배포가 실패한다.
(실제로 `book/index.html` 추가 때 이 사고가 났다.)

## 4. 저장소에 없는 것

- `data/` — Sanity로 옮기기 전의 md 원본. **빌드에 쓰이지 않는다.** 남아있어도 무시할 것
- `CLAUDE.md` 외 로컬 설정 파일들

## 5. 알아두면 좋은 것

- 배포가 안 될 때는 서버 지연보다 워크플로우 실패를 먼저 의심할 것:
  `gh run list --repo contextand/typing-practice --workflow=deploy.yml`
- Sanity CLI는 Node 22 이상이 필요하다 (`nvm use 22`)
- macOS의 python.org 배포판은 SSL 인증서가 없어 `build.py`가 실패할 수 있다.
  `/Applications/Python 3.x/Install Certificates.command` 실행으로 해결
- 변경 후에는 `python3 build.py` → 로컬 서버로 브라우저 확인까지 하고 커밋할 것

---

# 작업 방식

일반적인 코딩 태도에 관한 지침. 위의 프로젝트 맥락과 함께 적용한다.

**트레이드오프:** 속도보다 신중함 쪽으로 치우쳐 있다. 사소한 작업에는 재량껏 판단할 것.

## 1. 코딩 전에 생각하기

**가정하지 말 것. 혼란을 감추지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:

- 가정을 명시적으로 말할 것. 불확실하면 물어볼 것
- 해석이 여러 갈래면 제시할 것 — 말없이 하나를 고르지 말 것
- 더 단순한 방법이 있으면 말할 것. 필요하면 반대 의견을 낼 것
- 불명확하면 멈출 것. 무엇이 헷갈리는지 짚고 물어볼 것

## 2. 단순함 우선

**문제를 푸는 최소한의 코드. 추측성 구현 금지.**

- 요청받지 않은 기능 금지
- 한 번만 쓰는 코드에 추상화 금지
- 요청하지 않은 "유연성"이나 "설정 가능성" 금지
- 일어날 수 없는 상황에 대한 예외 처리 금지
- 200줄을 썼는데 50줄로 가능하다면 다시 쓸 것

스스로 물을 것: "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화한다.

## 3. 외과적 변경

**꼭 필요한 것만 건드릴 것. 내가 어지른 것만 치울 것.**

기존 코드를 수정할 때:

- 주변 코드·주석·포매팅을 "개선"하지 말 것
- 망가지지 않은 것을 리팩터링하지 말 것
- 내 취향과 다르더라도 기존 스타일을 따를 것
- 무관한 죽은 코드를 발견하면 언급만 하고 지우지 말 것

내 변경이 고아를 만들었을 때:

- 내 변경 때문에 쓰이지 않게 된 import·변수·함수는 제거할 것
- 원래부터 죽어있던 코드는 요청받지 않는 한 건드리지 말 것

기준: 바뀐 모든 줄이 사용자의 요청으로 곧장 이어져야 한다.

---

**이 지침이 작동하고 있다는 신호:** diff에 불필요한 변경이 줄어들고, 과설계로 인한 재작업이 줄어들며,
실수한 뒤가 아니라 구현 전에 확인 질문이 나온다.
